#include <stdio.h>
/*
 *
 * main- Entry point
 * printf- Print string
 * print- proper string
 * Return-always 0 (success)
 */
int main(void)
{
int print = "with proper grammar, but the outcome is a piece of art,"
printf ("%d", print)
return (0)
}

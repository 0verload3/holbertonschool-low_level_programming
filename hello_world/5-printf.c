#include <stdio.h>
/*
 * main-Entry point
 * printf-Print string
 * Return-always 0 (success)
 */int main(void)
{
printf("%d", "With proper grammar, but the outcome is a piece of ar,\n");
return (0);
}
